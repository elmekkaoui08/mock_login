import 'package:flutter/material.dart';

class CameraWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Camera Widget'),
      ),
    );
  }
}
